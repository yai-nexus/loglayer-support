# LogLayer Support - 企业级日志解决方案项目说明

## 📋 项目概述

**LogLayer Support** 是一个企业级日志解决方案的 Monorepo 项目，提供模块化的日志功能，支持多种前端和后端框架。项目采用现代化的开发工具链和最佳实践，为企业级应用提供开箱即用的日志解决方案。

- **项目名称**: loglayer-support-monorepo
- **版本**: v0.8.0
- **许可证**: MIT
- **作者**: YAI Nexus Team
- **仓库**: https://github.com/yai-nexus/loglayer-support

## 🛠️ 技术栈

### 核心技术

- **语言**: TypeScript 5.0+
- **运行时**: Node.js 18.0+
- **包管理器**: pnpm 8.0+ (推荐 pnpm@10.13.0)
- **构建工具**: Turbo (Turborepo) + tsup
- **测试框架**: Jest 29.0+
- **代码规范**: ESLint + Prettier

### 前端技术栈

- **React**: 19.1.0+
- **Next.js**: 15.4.2+
- **TypeScript**: 严格模式配置
- **构建目标**: ES2020
- **模块系统**: ESNext with bundler resolution

### 开发工具

- **Monorepo 管理**: Turborepo
- **包构建**: tsup (基于 esbuild)
- **代码格式化**: Prettier
- **代码检查**: ESLint with TypeScript
- **测试环境**: Jest (Node.js + jsdom)
- **E2E 测试**: Playwright
- **版本管理**: Changesets
- **CI/CD**: GitHub Actions

### 核心依赖

- **loglayer**: ^6.6.0 (核心日志库)
- **@loglayer/plugin-redaction**: ^2.0.0 (数据脱敏插件)

## 📁 项目结构

```
loglayer-support/
├── packages/                    # 核心包目录
│   ├── core/                   # 核心类型定义和工具
│   ├── browser/                # 浏览器端日志模块
│   ├── server/                 # 服务端日志模块
│   ├── next/                   # Next.js 专用模块
│   ├── receiver/               # 日志接收器模块
│   └── sls-transport/          # 阿里云 SLS 传输模块
├── examples/                   # 示例项目
│   ├── basic-example/          # 基础使用示例
│   ├── nextjs-example/         # Next.js 示例
│   └── react-example/          # React 示例
├── tools/                      # 开发工具
│   ├── build-config/           # 构建配置
│   └── eslint-config/          # ESLint 配置
├── scripts/                    # 脚本文件
├── tests/                      # E2E 测试
└── docs/                       # 文档
```

## 🚀 环境搭建

### 系统要求

- **Node.js**: >= 18.0.0
- **pnpm**: >= 8.0.0 (推荐使用 10.13.0)
- **操作系统**: macOS, Linux, Windows

### 安装步骤

1. **克隆项目**
   ```bash
   git clone https://github.com/yai-nexus/loglayer-support.git
   cd loglayer-support
   ```

2. **安装 pnpm** (如果未安装)
   ```bash
   npm install -g pnpm@10.13.0
   ```

3. **安装依赖**
   ```bash
   pnpm install
   ```

4. **构建所有包**
   ```bash
   pnpm build
   ```

5. **运行类型检查**
   ```bash
   pnpm type-check
   ```

6. **运行测试**
   ```bash
   pnpm test
   ```

## 🎯 启动方式

### 开发模式

1. **启动所有包的开发模式**
   ```bash
   pnpm dev
   ```

2. **启动特定示例项目**
   ```bash
   # React 示例
   pnpm dev:react
   
   # Next.js 示例 (端口 3001)
   pnpm dev:nextjs
   
   # 基础示例
   pnpm dev:basic
   ```

3. **使用脚本启动 Next.js 示例**
   ```bash
   ./scripts/run-nextjs-example.sh
   ```

### 构建和发布

1. **构建所有包**
   ```bash
   pnpm build
   ```

2. **仅构建核心包**
   ```bash
   pnpm build:packages
   ```

3. **版本管理**
   ```bash
   # 创建变更集
   pnpm changeset
   
   # 更新版本
   pnpm changeset:version
   
   # 发布到 npm
   pnpm changeset:publish
   ```

4. **发布 Beta 版本**
   ```bash
   pnpm release:beta
   ```

5. **发布正式版本**
   ```bash
   pnpm release:latest
   ```

### 代码质量检查

1. **代码检查**
   ```bash
   pnpm lint
   ```

2. **自动修复**
   ```bash
   pnpm lint:fix
   ```

3. **代码格式化**
   ```bash
   pnpm format
   ```

4. **格式检查**
   ```bash
   pnpm format:check
   ```

### 测试

1. **运行所有测试**
   ```bash
   pnpm test
   ```

2. **仅测试核心包**
   ```bash
   pnpm test:packages
   ```

3. **E2E 测试**
   ```bash
   cd tests/e2e
   npx playwright test
   ```

## 📦 可用模块

### 核心模块

- **@yai-loglayer/core**: 核心类型定义和工具函数
- **@yai-loglayer/browser**: 浏览器端日志解决方案
- **@yai-loglayer/server**: 服务端日志解决方案
- **@yai-loglayer/next**: Next.js 专用日志模块
- **@yai-loglayer/receiver**: 通用日志接收器
- **@yai-loglayer/sls-transport**: 阿里云 SLS 传输组件

### 安装使用

```bash
# 按需安装
npm install @yai-loglayer/core
npm install @yai-loglayer/browser
npm install @yai-loglayer/server
```

## 🔧 配置说明

### TypeScript 配置

- **目标**: ES2020
- **模块**: ESNext
- **严格模式**: 启用
- **声明文件**: 自动生成
- **Source Map**: 启用

### ESLint 配置

- **解析器**: @typescript-eslint/parser
- **规则**: 推荐 + TypeScript 推荐
- **代码风格**: Prettier 集成
- **环境**: Node.js + Jest

### Prettier 配置

- **分号**: 启用
- **单引号**: 启用
- **行宽**: 100 字符
- **缩进**: 2 空格
- **尾随逗号**: ES5 兼容

### Turbo 配置

- **构建缓存**: 启用
- **并行构建**: 支持
- **依赖管理**: 自动处理
- **输出目录**: dist/, .next/, build/

## 🌟 特性亮点

### 模块化架构

- **按需安装**: 减少包体积
- **独立版本**: 精确控制依赖
- **Tree-shaking**: 优化打包体积

### 企业级特性

- **智能错误处理**: 自动错误恢复
- **性能优化**: 序列化性能提升 74%
- **内存优化**: 内存使用减少 35%
- **健康检查**: 实时监控日志系统状态

### 开发体验

- **开箱即用**: 30 秒完成配置
- **类型安全**: 完整的 TypeScript 支持
- **热重载**: 开发模式实时更新
- **详细文档**: 每个包都有完整文档

## 🤝 贡献指南

1. **Fork 项目**
2. **创建特性分支**: `git checkout -b feature/amazing-feature`
3. **提交更改**: `git commit -m 'Add amazing feature'`
4. **推送分支**: `git push origin feature/amazing-feature`
5. **创建 Pull Request**

详细贡献指南请参考 [CONTRIBUTING.md](./CONTRIBUTING.md)

## 📄 许可证

本项目采用 MIT 许可证 - 详情请参考 [LICENSE](./LICENSE) 文件。

## 🔗 相关链接

- [项目仓库](https://github.com/yai-nexus/loglayer-support)
- [NPM 包](https://www.npmjs.com/package/@yai-loglayer/core)
- [文档](./docs/)
- [更新日志](./CHANGELOG.md)
- [迁移指南](./MIGRATION.md)

---

**注意**: 本项目正在积极开发中，API 可能会有变化。建议在生产环境使用前仔细测试。